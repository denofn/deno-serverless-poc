{
  "version": "1.0.1",
  "scripts": {
    "build": "yarn dvm build",
    "clean": "yarn dvm clean",
    "deploy": "gcloud builds submit --config cloudbuild.yaml .",
    "deno": "DENO_DIR=.deno .deno/bin/deno",
    "dvm": "./dvm",
    "launch": "yarn build && ./launch",
    "postinstall": "yarn dvm bootstrap"
  },
  "dependencies": {
    "deno_ls_plugin": "^0.1.0",
    "typescript": "^3.5.2"
  },
  "license": "MIT"
}
