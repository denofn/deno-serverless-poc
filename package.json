{
  "version": "1.0.1",
  "scripts": {
    "build": "yarn dvm build",
    "clean": "yarn dvm clean",
    "compile": "yarn deno bundle helloWorld.ts out.js",
    "deno": "DENO_DIR=.deno .deno/bin/deno",
    "dvm": "./dvm",
    "launch": "yarn compile && ./launch",
    "postinstall": "yarn dvm bootstrap",
    "publish": "gcloud builds submit --config cloudbuild.yaml ."
  },
  "dependencies": {
    "deno_ls_plugin": "^0.1.0",
    "typescript": "^3.5.2"
  },
  "license": "MIT"
}
